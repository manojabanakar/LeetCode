class Solution:
    def countElements(self, arr: List[int]) -> int:
        count = 0
        element_set = set(arr)  
        for element in arr:
            if element + 1 in element_set:  
                count += 1
        
        return count  
